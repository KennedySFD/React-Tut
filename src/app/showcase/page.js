import Overview from '@/showcase/Overview';

export const metadata = {
  title: 'Overview',
  description: 'Every component in the library, grouped by category.',
};

export default function ShowcasePage() {
  return <Overview />;
}
